# 🚀 Enterprise HRMS Data Purge & Cross-Validation Solution
### 💡 企业级 HRMS 历史数据自动化清洗与勾稽校验解决方案

![Python](https://img.shields.io/badge/Python-3.8%2B-blue?logo=python)
![Pandas](https://img.shields.io/badge/Pandas-2.0%2B-orange?logo=pandas)
![Implementation](https://img.shields.io/badge/Role-Implementation%20Consultant-green)
![License](https://img.shields.io/badge/License-MIT-purple)

在大型企业信息化系统（如 ERP、HRMS）实施交付的**历史数据迁移与初始化**阶段，多源头 Excel 表格往往存在格式混乱、关键字段缺失、甚至逻辑冲突（如登记性别与身份证不符）等行业痛点。手工核对不仅耗时，且极易引发上线后的系统级异常。

本项目是专为**系统实施顾问 / 数据分析师**打造的自动化数据治理工具集。基于 **Pipeline（流水线）** 设计思维，通过数据探测、自动化去噪、以及基于国家标准的业务逻辑深度校验，将数天的数据核对工作缩短至**秒级**完成。

---

## ✨ 核心亮点 (Key Features)

* **📊 零破坏资产探测**：在不污染源数据的前提下，宏观扫描数据集规模，全自动化输出各维度缺失值（`NaN`）分布报告，为制定清洗规则提供直观依据。
* **🧹 智能文本去噪机制**：
  * 全局剔除整列全空的无效字段，自动完成文本字段首尾空格修剪。
  * 利用正则表达式（`\s+`）深度清除姓名中间的异常空格（防止后续模糊查询失效）。
  * 阻断并统一清洗历史遗留脏数据（如：将无效输入 `3`、`未知` 自动转化为标准的 `None`/`Null`）。
* **⏳ 时间戳强转与阻断**：对“参加工作时间”、“入职日期”等高频关键时间节点进行标准化转换，对恶性格式进行 `coerce` 阻断打标，防止写库报错。
* **🔍 增量问题打标机制 (Labeling System)**：首创“数据问题”动态标签列。对“唯一标识缺失”、“入职日期缺失”、“证件号重复”的异常行进行**增量合并打标**（如：`证件号缺失;入职日期缺失;`），便于直接导出给客户源头修正。
* **🛡️ 基于国标的跨字段勾稽校验 (Cross-Validation)**：
  * **合法性前置过滤**：利用严格正则 `^\d{17}[\dXx]$` 拦截异常证件号。
  * **高精度年龄反推**：动态切片身份证 7~14 位，结合当前系统时间引入闰年权重（`365.25`）动态计算精确年龄。
  * **性别双向交叉验证**：提取身份证第 17 位特征码（奇男偶女），与原有登记性别自动进行比对。一旦发生冲突，秒级打标 `与身份证不符`，拦截隐蔽逻辑错误。

---

## 📐 解决方案架构 (Solution Pipeline)

项目遵循规范的现代数据工程流水线（Data Pipeline）设计：

[原始 Excel 数据源]
│
▼
1️⃣ explore.py ────► [数据资产探测报告] (识别缺失分布与规模)
│
▼
2️⃣ clean.py   ────► [自动化去噪与打标] (格式统一 / 时间强转 / 重复项标记)
│
▼
3️⃣ add_age.py ────► [业务逻辑深度校验] (身份证反推年龄 / 性别勾稽交叉验证)
│
▼
[高质量标准化写库数据] + [源头数据缺陷治理报告]


---

## 📂 模块详解 (Modules)

### 1. 数据探测器 `explore.py`
快速建立对原始数据集的“第一直觉”。通过跳过填表说明行（`skiprows`），直接透视底层数据矩阵的健康度。

### 2. 核心清洗引擎 `clean.py`
执行标准的数据清洗动作。通过构建增量字符串，将分散在不同维度的致命数据缺陷（如关键ID重复、缺失）浓缩在单一的 `数据问题` 列中。

### 3. 深度业务校验器 `add_age.py`
将技术与行业业务规则深度结合。利用中国居民身份证编码规范，反哺业务系统，自动完成数据富化（新增年龄列并调整至最优视觉字段顺序）与合规性拦截。

---

## 🚀 业务价值与落地效果 (Business Value)

1. **大幅提升交付效能**：将传统实施交付中数天的人工 VLOOKUP 筛查工作，降维打击至**秒级自动化输出**，显著缩短项目工期。
2. **零风险系统上线**：在数据正式写入数据库（Staging to Production）前建立防波堤，提前拦截逻辑相悖的数据，避免上线后引发关联表 Bug。
3. **驱动客户数据治理**：生成的标记表格可直接作为“数据治理报告”交付客户 HR 部门，用技术手段赋能业务，极大地体现交付团队的专业度。

---

## ⚙️ 快速开始 (Quick Start)

### 依赖环境
* Python 3.8+
* Pandas
* openpyxl (用于处理 Excel 读写)

### 运行流水线
```bash
# 1. 查看数据概况
python explore.py

# 2. 执行基础清洗与异常打标
python clean.py

# 3. 执行身份证交叉校验与数据富化
python add_age.py
💡 本工具集来自于一线系统实施顾问的实战总结，旨在通过自动化脚本规范企业数字化转型中的关键一环。欢迎 Issue 与 PR！

⚡ 把低效的人工变成自动化工具（符合公司推行的数字化、标准化交付趋势）。

🧠 证明你不仅有技术（Python/Pandas），更懂业务（身份证编码规则、数据治理流程），是一个复合型的、具有极高培养潜力的实施顾问。

</details>
